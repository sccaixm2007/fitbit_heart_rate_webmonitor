{
  "name": "fitbit-smart-hr-monitor",
  "version": "1.0.0",
  "description": "Activity-aware Fitbit heart rate monitor with AI analysis",
  "scripts": {
    "dev": "live-server --port=5500 --host=127.0.0.1 --no-browser=false",
    "start": "live-server --port=5500 --host=127.0.0.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
